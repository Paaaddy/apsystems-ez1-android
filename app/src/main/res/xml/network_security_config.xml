<?xml version="1.0" encoding="utf-8"?>
<!--
    Allow cleartext HTTP for local network communication with the EZ1 inverter.
    The EZ1 local API does not support HTTPS. This config permits cleartext only
    for private IP ranges (RFC 1918) — not the public internet.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
    </domain-config>
    <!-- Fallback: allow cleartext for any host (needed since IP is user-configured) -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
